# Copy to Vercel → Settings → Environment Variables.
# NEVER commit real secrets. Values differ per project and per environment.

APP_NAME=operations-api
APP_ENV=preview
APP_SECRET=replace-with-a-long-random-string
